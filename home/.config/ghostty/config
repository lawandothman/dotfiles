theme = Rose Pine
background = 0D1017
cursor-color = B62EB2

font-family = "Berkeley Mono"
font-family = "Symbols Nerd Font Mono"
font-size = 13

# Named explicitly because the slanted faces are called Oblique and their files
# leave the italic bit unset, so style matching cannot find them on its own.
font-style = Regular
font-style-italic = Oblique
font-style-bold = Bold
font-style-bold-italic = Bold Oblique
font-feature = -liga
font-feature = -calt
font-feature = -dlig
shell-integration-features = no-cursor,sudo,no-title

adjust-cell-height = 1

macos-titlebar-style = tabs
title = " "

# Left Option sends Alt so herdr's prefix+alt+N bindings fire. Right Option
# still composes, which the British layout needs for # (option+3).
macos-option-as-alt = left

mouse-hide-while-typing = true

window-save-state = always

window-padding-balance = true

quit-after-last-window-closed = true

custom-shader = shaders/cursor-trail.glsl
